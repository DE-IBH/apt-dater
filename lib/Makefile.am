
EXTRA_DISTS=cmd hosts2xml

install-exec-hook:
	$(mkinstalldirs) $(DESTDIR)$(pkglibdir)
	$(INSTALL) cmd $(DESTDIR)$(pkglibdir)
	$(INSTALL) hosts2xml $(DESTDIR)$(pkglibdir)
	$(INSTALL) ssh-addonce $(DESTDIR)$(pkglibdir)
